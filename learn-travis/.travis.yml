language: java
sudo: required
jdk:
- openjdk8

install:
-mvn -N io.takari:maven:wrapper

script:
- ./mvnw clean install

deploy:
- provider: heroku
  api_key: $HEROKU_API_KEY
  app: travis-service
  skip_clenup: True
  on:
    branch: master
